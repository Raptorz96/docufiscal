# Dark Mode Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Aggiungere dark mode all'intera app con toggle iOS nella navbar, persistenza localStorage, e theming completo di tutti i componenti.

**Architecture:** `darkMode: 'class'` in Tailwind — la classe `.dark` viene aggiunta/rimossa dal `<html>` dall'hook `useTheme`. Il toggle viene montato in `AppLayout.tsx` (l'unica istanza del hook). Tutte le pagine/componenti ricevono varianti `dark:` Tailwind sulle classi colore. Uno script inline in `index.html` applica la classe prima del bundle per evitare flash.

**Tech Stack:** React + TypeScript, Tailwind CSS `darkMode: 'class'`, localStorage

---

## File Map

| File | Azione |
|------|--------|
| `frontend/tailwind.config.js` | MODIFICA — aggiungi `darkMode: 'class'` |
| `frontend/src/hooks/useTheme.ts` | CREA — hook theme/toggle |
| `frontend/src/components/ThemeToggle.tsx` | CREA — pill toggle iOS |
| `frontend/src/layouts/AppLayout.tsx` | MODIFICA — hook + toggle + dark classes header/main |
| `frontend/index.html` | MODIFICA — script anti-flash |
| `frontend/src/index.css` | MODIFICA — dark body base |
| `frontend/src/pages/LoginPage.tsx` | MODIFICA — dark classes |
| `frontend/src/pages/RegisterPage.tsx` | MODIFICA — dark classes |
| `frontend/src/pages/DashboardPage.tsx` | MODIFICA — dark classes |
| `frontend/src/pages/ScadenzePage.tsx` | MODIFICA — dark classes |
| `frontend/src/pages/ClientiPage.tsx` | MODIFICA — dark classes |
| `frontend/src/pages/ContrattiPage.tsx` | MODIFICA — dark classes |
| `frontend/src/pages/DocumentiPage.tsx` | MODIFICA — dark classes |
| `frontend/src/pages/ProfilePage.tsx` | MODIFICA — dark classes (inputClass, labelClass, btnSecondary, sezioni) |
| `frontend/src/components/GoogleCalendarBanner.tsx` | MODIFICA — dark classes |
| `frontend/src/components/DocumentChatbot.tsx` | MODIFICA — dark classes |
| `frontend/src/components/ClienteFormModal.tsx` | MODIFICA — dark classes modale/form |
| `frontend/src/components/ContrattoFormModal.tsx` | MODIFICA — dark classes modale/form |
| `frontend/src/components/UploadDocumentoModal.tsx` | MODIFICA — dark classes modale/form |
| `frontend/src/components/ClassificazioneModal.tsx` | MODIFICA — dark classes modale/form |
| `frontend/src/components/Omnibox.tsx` | MODIFICA — dark classes |
| `frontend/src/components/PdfDrawer.tsx` | MODIFICA — dark classes drawer |

---

## Task 1: Tailwind darkMode + useTheme hook + ThemeToggle component

**Files:**
- Modify: `frontend/tailwind.config.js`
- Create: `frontend/src/hooks/useTheme.ts`
- Create: `frontend/src/components/ThemeToggle.tsx`

- [ ] **Step 1: Aggiungere `darkMode: 'class'` a tailwind.config.js**

```js
/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: 'class',
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
```

- [ ] **Step 2: Creare `frontend/src/hooks/useTheme.ts`**

```typescript
import { useState, useEffect } from 'react';

type Theme = 'light' | 'dark';

export function useTheme() {
  const [theme, setTheme] = useState<Theme>(() => {
    const saved = localStorage.getItem('docufiscal-theme') as Theme | null;
    if (saved === 'light' || saved === 'dark') return saved;
    if (window.matchMedia('(prefers-color-scheme: dark)').matches) return 'dark';
    return 'light';
  });

  useEffect(() => {
    const root = document.documentElement;
    if (theme === 'dark') {
      root.classList.add('dark');
    } else {
      root.classList.remove('dark');
    }
    localStorage.setItem('docufiscal-theme', theme);
  }, [theme]);

  const toggleTheme = () => setTheme(prev => prev === 'dark' ? 'light' : 'dark');

  return { theme, toggleTheme };
}
```

- [ ] **Step 3: Creare `frontend/src/components/ThemeToggle.tsx`**

```tsx
import React from 'react';

interface ThemeToggleProps {
  theme: 'light' | 'dark';
  onToggle: () => void;
}

const ThemeToggle: React.FC<ThemeToggleProps> = ({ theme, onToggle }) => (
  <button
    onClick={onToggle}
    className={`relative inline-flex h-6 w-11 items-center rounded-full transition-colors duration-200 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2 ${
      theme === 'dark' ? 'bg-blue-600' : 'bg-gray-300'
    }`}
    aria-label="Cambia tema"
    title={theme === 'dark' ? 'Passa al tema chiaro' : 'Passa al tema scuro'}
  >
    <span
      className={`inline-block h-4 w-4 transform rounded-full bg-white shadow transition-transform duration-200 ${
        theme === 'dark' ? 'translate-x-6' : 'translate-x-1'
      }`}
    />
    <span className="sr-only">{theme === 'dark' ? '🌙' : '☀️'}</span>
  </button>
);

export default ThemeToggle;
```

- [ ] **Step 4: Commit**

```bash
git add frontend/tailwind.config.js frontend/src/hooks/useTheme.ts frontend/src/components/ThemeToggle.tsx
git commit -m "feat: dark mode infrastructure — tailwind class strategy, useTheme hook, ThemeToggle component"
```

---

## Task 2: Anti-flash script + CSS base + AppLayout integration

**Files:**
- Modify: `frontend/index.html`
- Modify: `frontend/src/index.css`
- Modify: `frontend/src/layouts/AppLayout.tsx`

- [ ] **Step 1: Aggiungere script anti-flash in `index.html` (prima del bundle)**

Sostituire:
```html
  <body class="h-full">
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
```

Con:
```html
  <body class="h-full">
    <script>
      (function() {
        var t = localStorage.getItem('docufiscal-theme');
        if (t === 'dark' || (!t && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
          document.documentElement.classList.add('dark');
        }
      })();
    </script>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
```

- [ ] **Step 2: Aggiungere colore base dark in `index.css`**

Sostituire:
```css
@tailwind base;
@tailwind components;
@tailwind utilities;

html, body, #root {
  height: 100%;
}
```

Con:
```css
@tailwind base;
@tailwind components;
@tailwind utilities;

html, body, #root {
  height: 100%;
}

html.dark {
  background-color: #111827; /* gray-900 */
  color-scheme: dark;
}
```

- [ ] **Step 3: Integrare useTheme e ThemeToggle in `AppLayout.tsx`**

Aggiungere gli import dopo gli import esistenti:

Trovare:
```tsx
import { PdfDrawer } from '../components/PdfDrawer';
import { DocumentChatbot } from '../components/DocumentChatbot';
import { Omnibox } from '../components/Omnibox';
```

Sostituire con:
```tsx
import { PdfDrawer } from '../components/PdfDrawer';
import { DocumentChatbot } from '../components/DocumentChatbot';
import { Omnibox } from '../components/Omnibox';
import { useTheme } from '../hooks/useTheme';
import ThemeToggle from '../components/ThemeToggle';
```

- [ ] **Step 4: Chiamare useTheme nel componente AppLayout**

Trovare:
```tsx
  const [sidebarOpen, setSidebarOpen] = useState(false);
  const [desktopSidebarOpen, setDesktopSidebarOpen] = useState(true);
  const { user, logout } = useAuth();
```

Sostituire con:
```tsx
  const [sidebarOpen, setSidebarOpen] = useState(false);
  const [desktopSidebarOpen, setDesktopSidebarOpen] = useState(true);
  const { theme, toggleTheme } = useTheme();
  const { user, logout } = useAuth();
```

- [ ] **Step 5: Aggiungere dark classes al layout principale e inserire ThemeToggle nell'header**

Trovare:
```tsx
  return (
    <div className="flex h-screen bg-gray-50">
```

Sostituire con:
```tsx
  return (
    <div className="flex h-screen bg-gray-50 dark:bg-gray-900">
```

Trovare:
```tsx
        <header className="flex items-center gap-4 px-4 py-3 bg-white border-b border-gray-200 flex-shrink-0">
```

Sostituire con:
```tsx
        <header className="flex items-center gap-4 px-4 py-3 bg-white dark:bg-gray-800 border-b border-gray-200 dark:border-gray-700 flex-shrink-0">
```

Trovare:
```tsx
          <span className="font-bold text-slate-800 tracking-tight">DocuFiscal</span>
```

Sostituire con:
```tsx
          <span className="font-bold text-slate-800 dark:text-gray-100 tracking-tight">DocuFiscal</span>
```

Trovare:
```tsx
            onClick={() => setSidebarOpen(true)}
            className="md:hidden p-1.5 rounded-lg text-slate-600 hover:bg-slate-100 transition-colors"
```

Sostituire con:
```tsx
            onClick={() => setSidebarOpen(true)}
            className="md:hidden p-1.5 rounded-lg text-slate-600 dark:text-gray-400 hover:bg-slate-100 dark:hover:bg-gray-700 transition-colors"
```

Trovare:
```tsx
            className="hidden md:flex p-1.5 rounded-lg text-slate-600 hover:bg-slate-100 transition-colors"
```

Sostituire con:
```tsx
            className="hidden md:flex p-1.5 rounded-lg text-slate-600 dark:text-gray-400 hover:bg-slate-100 dark:hover:bg-gray-700 transition-colors"
```

Trovare (la chiusura dell'Omnibox div):
```tsx
          </div>
        </header>
```

Sostituire con:
```tsx
          </div>
          <ThemeToggle theme={theme} onToggle={toggleTheme} />
        </header>
```

- [ ] **Step 6: Commit**

```bash
git add frontend/index.html frontend/src/index.css frontend/src/layouts/AppLayout.tsx
git commit -m "feat: dark mode — anti-flash script, AppLayout toggle integration"
```

---

## Task 3: LoginPage + RegisterPage dark mode

**Files:**
- Modify: `frontend/src/pages/LoginPage.tsx`
- Modify: `frontend/src/pages/RegisterPage.tsx`

- [ ] **Step 1: Dark mode LoginPage**

Trovare:
```tsx
    <div className="min-h-screen flex items-center justify-center bg-gray-50">
```

Sostituire con:
```tsx
    <div className="min-h-screen flex items-center justify-center bg-gray-50 dark:bg-gray-900">
```

Trovare:
```tsx
          <h2 className="mt-6 text-center text-3xl font-extrabold text-gray-900">
            Accedi a DocuFiscal
          </h2>
```

Sostituire con:
```tsx
          <h2 className="mt-6 text-center text-3xl font-extrabold text-gray-900 dark:text-gray-100">
            Accedi a DocuFiscal
          </h2>
```

Trovare (input email):
```tsx
                className="appearance-none rounded-none relative block w-full px-3 py-2 border border-gray-300 placeholder-gray-500 text-gray-900 rounded-t-md focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 focus:z-10 sm:text-sm"
```

Sostituire con:
```tsx
                className="appearance-none rounded-none relative block w-full px-3 py-2 border border-gray-300 dark:border-gray-600 dark:bg-gray-700 dark:text-gray-100 dark:placeholder-gray-400 placeholder-gray-500 text-gray-900 rounded-t-md focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 focus:z-10 sm:text-sm"
```

Trovare (input password):
```tsx
                className="appearance-none rounded-none relative block w-full px-3 py-2 border border-gray-300 placeholder-gray-500 text-gray-900 rounded-b-md focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 focus:z-10 sm:text-sm"
```

Sostituire con:
```tsx
                className="appearance-none rounded-none relative block w-full px-3 py-2 border border-gray-300 dark:border-gray-600 dark:bg-gray-700 dark:text-gray-100 dark:placeholder-gray-400 placeholder-gray-500 text-gray-900 rounded-b-md focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 focus:z-10 sm:text-sm"
```

Trovare:
```tsx
          <div className="text-center text-sm text-gray-600">
```

Sostituire con:
```tsx
          <div className="text-center text-sm text-gray-600 dark:text-gray-400">
```

- [ ] **Step 2: Dark mode RegisterPage**

Il file `RegisterPage.tsx` ha lo stesso pattern (`bg-gray-50`, `text-gray-900`, input con `border-gray-300`). Applicare:

Trovare:
```tsx
    <div className="min-h-screen flex items-center justify-center bg-gray-50">
```

Sostituire con:
```tsx
    <div className="min-h-screen flex items-center justify-center bg-gray-50 dark:bg-gray-900">
```

Poi leggere il resto del JSX e applicare `dark:text-gray-100` ai titoli, `dark:text-gray-400` ai testi secondari, e `dark:border-gray-600 dark:bg-gray-700 dark:text-gray-100` a tutti gli input.

- [ ] **Step 3: Commit**

```bash
git add frontend/src/pages/LoginPage.tsx frontend/src/pages/RegisterPage.tsx
git commit -m "feat: dark mode — LoginPage and RegisterPage"
```

---

## Task 4: DashboardPage dark mode

**Files:**
- Modify: `frontend/src/pages/DashboardPage.tsx`

- [ ] **Step 1: Dark mode layout principale e card statistiche**

Trovare:
```tsx
    <div className="p-8 space-y-8 max-w-7xl mx-auto">
```

Sostituire con:
```tsx
    <div className="p-8 space-y-8 max-w-7xl mx-auto">
```
(nessun cambio — il main area dark è già gestito da AppLayout)

Trovare:
```tsx
            className={`relative group bg-white rounded-2xl border border-gray-100 p-6 shadow-sm hover:shadow-md transition-all duration-300 ${card.urgent ? 'ring-1 ring-amber-200' : ''}`}
```

Sostituire con:
```tsx
            className={`relative group bg-white dark:bg-gray-800 rounded-2xl border border-gray-100 dark:border-gray-700 p-6 shadow-sm hover:shadow-md transition-all duration-300 ${card.urgent ? 'ring-1 ring-amber-200' : ''}`}
```

Trovare:
```tsx
              <p className="text-sm font-medium text-gray-500">{card.title}</p>
              <h2 className="text-4xl font-bold text-gray-900 mt-1">{card.value}</h2>
```

Sostituire con:
```tsx
              <p className="text-sm font-medium text-gray-500 dark:text-gray-400">{card.title}</p>
              <h2 className="text-4xl font-bold text-gray-900 dark:text-gray-100 mt-1">{card.value}</h2>
```

- [ ] **Step 2: Dark mode sezione scadenze**

Trovare:
```tsx
      <div className="bg-white rounded-2xl border border-gray-100 shadow-sm overflow-hidden">
        <div className="p-6 border-b border-gray-50 flex items-center justify-between">
```

Sostituire con:
```tsx
      <div className="bg-white dark:bg-gray-800 rounded-2xl border border-gray-100 dark:border-gray-700 shadow-sm overflow-hidden">
        <div className="p-6 border-b border-gray-50 dark:border-gray-700 flex items-center justify-between">
```

Trovare:
```tsx
              <h2 className="text-xl font-bold text-gray-900">Scadenze (AI)</h2>
              <p className="text-xs text-gray-400 mt-0.5">Estratte automaticamente da documenti e contratti</p>
```

Sostituire con:
```tsx
              <h2 className="text-xl font-bold text-gray-900 dark:text-gray-100">Scadenze (AI)</h2>
              <p className="text-xs text-gray-400 dark:text-gray-500 mt-0.5">Estratte automaticamente da documenti e contratti</p>
```

- [ ] **Step 3: Dark mode ScadenzaCard**

Trovare:
```tsx
      className={`bg-white rounded-xl border ${borderColor} p-4 shadow-sm hover:shadow-md transition-all duration-200 cursor-pointer flex flex-col gap-3`}
```

Sostituire con:
```tsx
      className={`bg-white dark:bg-gray-800 rounded-xl border ${borderColor} p-4 shadow-sm hover:shadow-md transition-all duration-200 cursor-pointer flex flex-col gap-3`}
```

Trovare:
```tsx
          <p className="text-sm font-bold text-gray-900 truncate">{s.cliente_nome}</p>
          <p className="text-xs text-gray-400 truncate mt-0.5" title={s.file_name}>
```

Sostituire con:
```tsx
          <p className="text-sm font-bold text-gray-900 dark:text-gray-100 truncate">{s.cliente_nome}</p>
          <p className="text-xs text-gray-400 dark:text-gray-500 truncate mt-0.5" title={s.file_name}>
```

Trovare:
```tsx
          <div className="flex items-center gap-1.5 text-sm text-gray-500">
```

Sostituire con:
```tsx
          <div className="flex items-center gap-1.5 text-sm text-gray-500 dark:text-gray-400">
```

Trovare:
```tsx
        <div className="pt-2 mt-auto border-t border-gray-100">
```

Sostituire con:
```tsx
        <div className="pt-2 mt-auto border-t border-gray-100 dark:border-gray-700">
```

- [ ] **Step 4: Dark mode sezione ultimi documenti**

Trovare (seconda occorrenza di `bg-white rounded-2xl`):
```tsx
      <div className="bg-white rounded-2xl border border-gray-100 shadow-sm overflow-hidden">
        <div className="p-6 border-b border-gray-50 flex items-center justify-between">
          <div className="flex items-center space-x-3">
            <div className="p-2 bg-green-50 text-green-600 rounded-lg">
```

Sostituire con:
```tsx
      <div className="bg-white dark:bg-gray-800 rounded-2xl border border-gray-100 dark:border-gray-700 shadow-sm overflow-hidden">
        <div className="p-6 border-b border-gray-50 dark:border-gray-700 flex items-center justify-between">
          <div className="flex items-center space-x-3">
            <div className="p-2 bg-green-50 text-green-600 rounded-lg">
```

Trovare:
```tsx
                <tr className="bg-gray-50/50 text-gray-400 text-[10px] uppercase font-bold tracking-widest">
```

Sostituire con:
```tsx
                <tr className="bg-gray-50/50 dark:bg-gray-700/50 text-gray-400 dark:text-gray-500 text-[10px] uppercase font-bold tracking-widest">
```

Trovare:
```tsx
                    <tr key={doc.id} className="hover:bg-green-50/20 transition-colors">
```

Sostituire con:
```tsx
                    <tr key={doc.id} className="hover:bg-green-50/20 dark:hover:bg-gray-700/30 transition-colors">
```

Trovare:
```tsx
                      <div className="text-sm font-medium text-gray-900 max-w-xs truncate">{doc.file_name}</div>
```

Sostituire con:
```tsx
                      <div className="text-sm font-medium text-gray-900 dark:text-gray-100 max-w-xs truncate">{doc.file_name}</div>
```

Trovare (due occorrenze di `text-sm text-gray-600` nella tabella ultimi documenti, per tipo e cliente):
```tsx
                      <div className="text-sm text-gray-600">{doc.tipo_documento}</div>
```
Sostituire con:
```tsx
                      <div className="text-sm text-gray-600 dark:text-gray-400">{doc.tipo_documento}</div>
```

Trovare:
```tsx
                      <div className="text-sm text-gray-600">{doc.cliente_nome}</div>
```
Sostituire con:
```tsx
                      <div className="text-sm text-gray-600 dark:text-gray-400">{doc.cliente_nome}</div>
```

Trovare:
```tsx
                    <td className="px-6 py-4 text-sm text-gray-500">
```
Sostituire con:
```tsx
                    <td className="px-6 py-4 text-sm text-gray-500 dark:text-gray-400">
```

- [ ] **Step 5: Commit**

```bash
git add frontend/src/pages/DashboardPage.tsx
git commit -m "feat: dark mode — DashboardPage"
```

---

## Task 5: ScadenzePage dark mode

**Files:**
- Modify: `frontend/src/pages/ScadenzePage.tsx`

- [ ] **Step 1: Dark mode header e filtri**

Trovare:
```tsx
            <h1 className="text-3xl font-bold text-gray-900 tracking-tight">Scadenze</h1>
            <p className="text-gray-500 mt-1">
```

Sostituire con:
```tsx
            <h1 className="text-3xl font-bold text-gray-900 dark:text-gray-100 tracking-tight">Scadenze</h1>
            <p className="text-gray-500 dark:text-gray-400 mt-1">
```

Trovare:
```tsx
            <div className="bg-white rounded-2xl border border-gray-100 shadow-sm p-4 flex flex-wrap gap-3 items-center">
```

Sostituire con:
```tsx
            <div className="bg-white dark:bg-gray-800 rounded-2xl border border-gray-100 dark:border-gray-700 shadow-sm p-4 flex flex-wrap gap-3 items-center">
```

Trovare (input search):
```tsx
                    className="border border-gray-200 rounded-lg px-3 py-2 text-sm flex-1 min-w-[200px] focus:outline-none focus:ring-2 focus:ring-indigo-300"
```

Sostituire con:
```tsx
                    className="border border-gray-200 dark:border-gray-600 dark:bg-gray-700 dark:text-gray-100 dark:placeholder-gray-400 rounded-lg px-3 py-2 text-sm flex-1 min-w-[200px] focus:outline-none focus:ring-2 focus:ring-indigo-300"
```

Trovare (select tipo):
```tsx
                    className="border border-gray-200 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-indigo-300"
```

Sostituire con:
```tsx
                    className="border border-gray-200 dark:border-gray-600 dark:bg-gray-700 dark:text-gray-100 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-indigo-300"
```

Trovare:
```tsx
                    <label className="flex items-center gap-2 text-sm text-gray-600 cursor-pointer select-none">
```

Sostituire con:
```tsx
                    <label className="flex items-center gap-2 text-sm text-gray-600 dark:text-gray-400 cursor-pointer select-none">
```

- [ ] **Step 2: Dark mode tabella**

Trovare:
```tsx
            <div className="bg-white rounded-2xl border border-gray-100 shadow-sm overflow-hidden">
```

Sostituire con:
```tsx
            <div className="bg-white dark:bg-gray-800 rounded-2xl border border-gray-100 dark:border-gray-700 shadow-sm overflow-hidden">
```

Trovare:
```tsx
                                <tr className="bg-gray-50/50 text-gray-400 text-[10px] uppercase font-bold tracking-widest">
```

Sostituire con:
```tsx
                                <tr className="bg-gray-50/50 dark:bg-gray-700/50 text-gray-400 dark:text-gray-500 text-[10px] uppercase font-bold tracking-widest">
```

Trovare:
```tsx
                                            className={`hover:bg-gray-50/50 transition-colors cursor-pointer ${urgenzaRowClass(s.giorni_rimanenti)}`}
```

Sostituire con:
```tsx
                                            className={`hover:bg-gray-50/50 dark:hover:bg-gray-700/30 transition-colors cursor-pointer ${urgenzaRowClass(s.giorni_rimanenti)}`}
```

Trovare:
```tsx
                                            <td className="px-5 py-3 text-sm font-medium text-gray-900">{s.cliente_nome}</td>
```

Sostituire con:
```tsx
                                            <td className="px-5 py-3 text-sm font-medium text-gray-900 dark:text-gray-100">{s.cliente_nome}</td>
```

Trovare:
```tsx
                                            <td className="px-5 py-3 text-sm text-gray-600 max-w-xs truncate">{s.descrizione || '—'}</td>
```

Sostituire con:
```tsx
                                            <td className="px-5 py-3 text-sm text-gray-600 dark:text-gray-400 max-w-xs truncate">{s.descrizione || '—'}</td>
```

Trovare:
```tsx
                                            <td className="px-5 py-3 text-sm text-gray-600">
```

Sostituire (replace_all: true per tutte le occorrenze con data/canone):
```tsx
                                            <td className="px-5 py-3 text-sm text-gray-600 dark:text-gray-400">
```

Trovare:
```tsx
                                            <td className="px-5 py-3 text-xs text-gray-400 max-w-[150px] truncate" title={s.file_name}>
```

Sostituire con:
```tsx
                                            <td className="px-5 py-3 text-xs text-gray-400 dark:text-gray-500 max-w-[150px] truncate" title={s.file_name}>
```

- [ ] **Step 3: Commit**

```bash
git add frontend/src/pages/ScadenzePage.tsx
git commit -m "feat: dark mode — ScadenzePage"
```

---

## Task 6: ClientiPage + ContrattiPage dark mode

**Files:**
- Modify: `frontend/src/pages/ClientiPage.tsx`
- Modify: `frontend/src/pages/ContrattiPage.tsx`

- [ ] **Step 1: Dark mode ClientiPage**

Trovare (loading/error states):
```tsx
      <div className="min-h-screen bg-gray-50 flex items-center justify-center">
```
Sostituire (replace_all: true):
```tsx
      <div className="min-h-screen bg-gray-50 dark:bg-gray-900 flex items-center justify-center">
```

Trovare (root div):
```tsx
    <div className="min-h-screen bg-gray-50 py-8">
```
Sostituire con:
```tsx
    <div className="min-h-screen bg-gray-50 dark:bg-gray-900 py-8">
```

Trovare:
```tsx
            <h1 className="text-2xl font-semibold text-gray-900">Clienti</h1>
            <p className="mt-2 text-sm text-gray-700">Gestione dei clienti</p>
```
Sostituire con:
```tsx
            <h1 className="text-2xl font-semibold text-gray-900 dark:text-gray-100">Clienti</h1>
            <p className="mt-2 text-sm text-gray-700 dark:text-gray-400">Gestione dei clienti</p>
```

Trovare:
```tsx
          <div className="mt-6 bg-white shadow-sm rounded-lg">
            <div className="px-6 py-4 border-b border-gray-200">
```
Sostituire con:
```tsx
          <div className="mt-6 bg-white dark:bg-gray-800 shadow-sm rounded-lg">
            <div className="px-6 py-4 border-b border-gray-200 dark:border-gray-700">
```

Trovare (input ricerca):
```tsx
                  className="block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm"
```
Sostituire (replace_all: true — copre tutte le occorrenze in ClientiPage):
```tsx
                  className="block w-full rounded-md border-gray-300 dark:border-gray-600 dark:bg-gray-700 dark:text-gray-100 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm"
```

Trovare:
```tsx
                <thead className="bg-gray-50">
```
Sostituire (replace_all: true):
```tsx
                <thead className="bg-gray-50 dark:bg-gray-700">
```

Trovare:
```tsx
                    <th className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
```
Sostituire (replace_all: true):
```tsx
                    <th className="px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-gray-400 uppercase tracking-wider">
```

Trovare:
```tsx
                <tbody className="bg-white divide-y divide-gray-200">
```
Sostituire (replace_all: true):
```tsx
                <tbody className="bg-white dark:bg-gray-800 divide-y divide-gray-200 dark:divide-gray-700">
```

Trovare:
```tsx
                      <td className="px-6 py-4 whitespace-nowrap text-sm text-gray-500 font-mono">
```
Sostituire con:
```tsx
                      <td className="px-6 py-4 whitespace-nowrap text-sm text-gray-500 dark:text-gray-400 font-mono">
```

Trovare:
```tsx
                      <td className="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900">
```
Sostituire con:
```tsx
                      <td className="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900 dark:text-gray-100">
```

Trovare:
```tsx
                      <td className="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
```
Sostituire (replace_all: true):
```tsx
                      <td className="px-6 py-4 whitespace-nowrap text-sm text-gray-500 dark:text-gray-400">
```

- [ ] **Step 2: Dark mode ContrattiPage (stesse classi)**

Applicare le stesse trasformazioni di ClientiPage a ContrattiPage:
- `min-h-screen bg-gray-50` → `min-h-screen bg-gray-50 dark:bg-gray-900`
- `text-gray-900` (titoli) → `dark:text-gray-100`
- `text-gray-700` (descrizione) → `dark:text-gray-300`
- `bg-white shadow-sm rounded-lg` → `dark:bg-gray-800`
- `border-gray-200` → `dark:border-gray-700`
- `thead className="bg-gray-50"` → `dark:bg-gray-700`
- `text-gray-500 uppercase tracking-wider` → `dark:text-gray-400`
- `tbody className="bg-white divide-y divide-gray-200"` → `dark:bg-gray-800 dark:divide-gray-700`
- `text-sm font-medium text-gray-900` → `dark:text-gray-100`
- `text-sm text-gray-500` → `dark:text-gray-400`
- Banner upload verde: `bg-green-50 border border-green-200 text-green-800` → aggiungere `dark:bg-green-900/20 dark:border-green-800 dark:text-green-300`
- Select (label `text-gray-700 mb-1`) → `dark:text-gray-300`
- Select inputs: aggiungere `dark:border-gray-600 dark:bg-gray-700 dark:text-gray-100`
- Bottone secondario `bg-white border border-gray-300 text-gray-700 hover:bg-gray-50` → aggiungere dark variants

Trovare in ContrattiPage:
```tsx
          <div className="mt-4 sm:mt-0 sm:ml-16 sm:flex-none flex gap-2">
            <button
              onClick={() => setIsUploadOpen(true)}
              className="inline-flex items-center justify-center rounded-md border border-gray-300 bg-white px-4 py-2 text-sm font-medium text-gray-700 shadow-sm hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2 sm:w-auto"
```

Sostituire con:
```tsx
          <div className="mt-4 sm:mt-0 sm:ml-16 sm:flex-none flex gap-2">
            <button
              onClick={() => setIsUploadOpen(true)}
              className="inline-flex items-center justify-center rounded-md border border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-700 px-4 py-2 text-sm font-medium text-gray-700 dark:text-gray-200 shadow-sm hover:bg-gray-50 dark:hover:bg-gray-600 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2 sm:w-auto"
```

- [ ] **Step 3: Commit**

```bash
git add frontend/src/pages/ClientiPage.tsx frontend/src/pages/ContrattiPage.tsx
git commit -m "feat: dark mode — ClientiPage and ContrattiPage"
```

---

## Task 7: DocumentiPage dark mode

**Files:**
- Modify: `frontend/src/pages/DocumentiPage.tsx`

- [ ] **Step 1: Dark mode stati loading/error e root**

Trovare (replace_all: true):
```tsx
      <div className="min-h-screen bg-gray-50 flex items-center justify-center">
```
Sostituire con:
```tsx
      <div className="min-h-screen bg-gray-50 dark:bg-gray-900 flex items-center justify-center">
```

Trovare:
```tsx
    <div className="min-h-screen bg-gray-50 py-8">
```
Sostituire con:
```tsx
    <div className="min-h-screen bg-gray-50 dark:bg-gray-900 py-8">
```

- [ ] **Step 2: Dark mode header**

Trovare:
```tsx
            <h1 className="text-2xl font-semibold text-gray-900">Documenti</h1>
            <p className="mt-1 text-sm text-gray-700">Gestione avanzata dei documenti caricati</p>
```
Sostituire con:
```tsx
            <h1 className="text-2xl font-semibold text-gray-900 dark:text-gray-100">Documenti</h1>
            <p className="mt-1 text-sm text-gray-700 dark:text-gray-400">Gestione avanzata dei documenti caricati</p>
```

- [ ] **Step 3: Dark mode pannello filtri sticky**

Trovare:
```tsx
        <div className="sticky top-0 z-20 bg-white shadow-md rounded-xl border border-gray-200 mb-4">
```
Sostituire con:
```tsx
        <div className="sticky top-0 z-20 bg-white dark:bg-gray-800 shadow-md rounded-xl border border-gray-200 dark:border-gray-700 mb-4">
```

Trovare:
```tsx
            className="md:hidden w-full flex items-center justify-between px-4 py-3 text-sm font-medium text-gray-700 hover:bg-gray-50 transition-colors"
```
Sostituire con:
```tsx
            className="md:hidden w-full flex items-center justify-between px-4 py-3 text-sm font-medium text-gray-700 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-700 transition-colors"
```

Trovare:
```tsx
            <p className="text-xs font-semibold text-gray-400 uppercase tracking-wider mb-3">Filtri Backend</p>
```
Sostituire con:
```tsx
            <p className="text-xs font-semibold text-gray-400 dark:text-gray-500 uppercase tracking-wider mb-3">Filtri Backend</p>
```

Trovare:
```tsx
                <label className="block text-xs font-medium text-gray-600 mb-1">Cliente</label>
```
Sostituire (replace_all: true — copre tutte le label `text-xs font-medium text-gray-600`):
```tsx
                <label className="block text-xs font-medium text-gray-600 dark:text-gray-400 mb-1">Cliente</label>
```

Trovare (select con `border-gray-300`):
```tsx
                  className="block w-full rounded-lg border-gray-300 text-sm shadow-sm focus:border-indigo-500 focus:ring-indigo-500 disabled:bg-gray-100 disabled:cursor-not-allowed"
```
Sostituire con:
```tsx
                  className="block w-full rounded-lg border-gray-300 dark:border-gray-600 dark:bg-gray-700 dark:text-gray-100 text-sm shadow-sm focus:border-indigo-500 focus:ring-indigo-500 disabled:bg-gray-100 dark:disabled:bg-gray-600 disabled:cursor-not-allowed"
```

Trovare:
```tsx
            <div className="px-6 pt-4 pb-3 border-b border-gray-100">
```
Sostituire (replace_all: true):
```tsx
            <div className="px-6 pt-4 pb-3 border-b border-gray-100 dark:border-gray-700">
```

- [ ] **Step 4: Dark mode tabella documenti**

Trovare:
```tsx
          <div className="bg-white shadow-sm rounded-xl overflow-hidden border border-gray-200">
```
Sostituire con:
```tsx
          <div className="bg-white dark:bg-gray-800 shadow-sm rounded-xl overflow-hidden border border-gray-200 dark:border-gray-700">
```

Trovare (thead):
```tsx
                  className="bg-gray-50 text-gray-500 text-[10px] font-bold uppercase tracking-widest"
```
Sostituire con:
```tsx
                  className="bg-gray-50 dark:bg-gray-700 text-gray-500 dark:text-gray-400 text-[10px] font-bold uppercase tracking-widest"
```

Trovare (rows):
```tsx
                      className="hover:bg-gray-50/50 transition-colors group border-b border-gray-50 last:border-0"
```
Sostituire con:
```tsx
                      className="hover:bg-gray-50/50 dark:hover:bg-gray-700/30 transition-colors group border-b border-gray-50 dark:border-gray-700 last:border-0"
```

Trovare:
```tsx
                          className="text-sm font-medium text-gray-900 truncate max-w-[200px]"
```
Sostituire con:
```tsx
                          className="text-sm font-medium text-gray-900 dark:text-gray-100 truncate max-w-[200px]"
```

- [ ] **Step 5: Commit**

```bash
git add frontend/src/pages/DocumentiPage.tsx
git commit -m "feat: dark mode — DocumentiPage"
```

---

## Task 8: ProfilePage dark mode

**Files:**
- Modify: `frontend/src/pages/ProfilePage.tsx`

- [ ] **Step 1: Dark mode variabili inputClass, labelClass, btnSecondary**

Trovare:
```tsx
  const inputClass =
    'w-full px-3 py-2 border border-gray-300 rounded-md text-sm text-gray-900 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500';
  const labelClass = 'block text-sm font-medium text-gray-700 mb-1';
  const btnPrimary =
    'px-4 py-2 bg-indigo-600 text-white text-sm font-medium rounded-md hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 disabled:opacity-50 disabled:cursor-not-allowed';
  const btnSecondary =
    'px-4 py-2 bg-white text-gray-700 text-sm font-medium rounded-md border border-gray-300 hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 disabled:opacity-50 disabled:cursor-not-allowed';
```

Sostituire con:
```tsx
  const inputClass =
    'w-full px-3 py-2 border border-gray-300 dark:border-gray-600 dark:bg-gray-700 dark:text-gray-100 rounded-md text-sm text-gray-900 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500';
  const labelClass = 'block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1';
  const btnPrimary =
    'px-4 py-2 bg-indigo-600 text-white text-sm font-medium rounded-md hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 disabled:opacity-50 disabled:cursor-not-allowed';
  const btnSecondary =
    'px-4 py-2 bg-white dark:bg-gray-700 text-gray-700 dark:text-gray-200 text-sm font-medium rounded-md border border-gray-300 dark:border-gray-600 hover:bg-gray-50 dark:hover:bg-gray-600 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 disabled:opacity-50 disabled:cursor-not-allowed';
```

- [ ] **Step 2: Dark mode sezioni card**

Trovare (replace_all: true):
```tsx
      <div className="bg-white rounded-lg shadow-sm border border-gray-200 p-6 mb-6">
```
Sostituire con:
```tsx
      <div className="bg-white dark:bg-gray-800 rounded-lg shadow-sm border border-gray-200 dark:border-gray-700 p-6 mb-6">
```

Trovare (ultima sezione senza mb-6):
```tsx
      <div className="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
```
Sostituire con:
```tsx
      <div className="bg-white dark:bg-gray-800 rounded-lg shadow-sm border border-gray-200 dark:border-gray-700 p-6">
```

Trovare:
```tsx
      <h1 className="text-2xl font-bold text-gray-900 mb-6">Il mio profilo</h1>
```
Sostituire con:
```tsx
      <h1 className="text-2xl font-bold text-gray-900 dark:text-gray-100 mb-6">Il mio profilo</h1>
```

Trovare (replace_all: true):
```tsx
        <h2 className="text-lg font-semibold text-gray-800 mb-4">
```
Sostituire con:
```tsx
        <h2 className="text-lg font-semibold text-gray-800 dark:text-gray-200 mb-4">
```

Trovare:
```tsx
          <div className="grid grid-cols-2 gap-4 mb-6 p-4 bg-gray-50 rounded-md text-sm">
```
Sostituire con:
```tsx
          <div className="grid grid-cols-2 gap-4 mb-6 p-4 bg-gray-50 dark:bg-gray-700 rounded-md text-sm">
```

Trovare (replace_all: true):
```tsx
            <span className="font-medium text-gray-500">
```
Sostituire con:
```tsx
            <span className="font-medium text-gray-500 dark:text-gray-400">
```

Trovare (replace_all: true):
```tsx
            <p className="text-gray-800 mt-0.5
```
Sostituire con:
```tsx
            <p className="text-gray-800 dark:text-gray-200 mt-0.5
```

Trovare:
```tsx
        <p className="text-xs text-amber-600 mt-1">
```
(già colorato — ok)

Trovare (replace_all: true — alert boxes verdi):
```tsx
            <div className="bg-green-50 border border-green-200 text-green-800 text-sm rounded-md px-4 py-3">
```
Sostituire con:
```tsx
            <div className="bg-green-50 dark:bg-green-900/20 border border-green-200 dark:border-green-800 text-green-800 dark:text-green-300 text-sm rounded-md px-4 py-3">
```

Trovare (replace_all: true — alert boxes rosse):
```tsx
            <div className="bg-red-50 border border-red-200 text-red-800 text-sm rounded-md px-4 py-3">
```
Sostituire con:
```tsx
            <div className="bg-red-50 dark:bg-red-900/20 border border-red-200 dark:border-red-800 text-red-800 dark:text-red-300 text-sm rounded-md px-4 py-3">
```

Trovare:
```tsx
            <div className="w-9 h-9 flex items-center justify-center rounded-full bg-gray-100 shrink-0">
```
Sostituire con:
```tsx
            <div className="w-9 h-9 flex items-center justify-center rounded-full bg-gray-100 dark:bg-gray-700 shrink-0">
```

Trovare:
```tsx
              <p className="text-sm font-medium text-gray-800">Google Calendar</p>
```
Sostituire con:
```tsx
              <p className="text-sm font-medium text-gray-800 dark:text-gray-200">Google Calendar</p>
```

Trovare:
```tsx
        <p className="text-xs text-gray-400 mt-4">
```
Sostituire con:
```tsx
        <p className="text-xs text-gray-400 dark:text-gray-500 mt-4">
```

- [ ] **Step 3: Commit**

```bash
git add frontend/src/pages/ProfilePage.tsx
git commit -m "feat: dark mode — ProfilePage"
```

---

## Task 9: GoogleCalendarBanner + DocumentChatbot dark mode

**Files:**
- Modify: `frontend/src/components/GoogleCalendarBanner.tsx`
- Modify: `frontend/src/components/DocumentChatbot.tsx`

- [ ] **Step 1: Dark mode GoogleCalendarBanner**

Trovare:
```tsx
    <div className={`bg-blue-50 border border-blue-200 rounded-lg p-4 flex items-center gap-4 ${className ?? ''}`}>
```
Sostituire con:
```tsx
    <div className={`bg-blue-50 dark:bg-blue-900/20 border border-blue-200 dark:border-blue-800 rounded-lg p-4 flex items-center gap-4 ${className ?? ''}`}>
```

Trovare:
```tsx
      <p className="text-sm text-blue-800 flex-1">
```
Sostituire con:
```tsx
      <p className="text-sm text-blue-800 dark:text-blue-300 flex-1">
```

- [ ] **Step 2: Dark mode DocumentChatbot — chat window**

Trovare:
```tsx
                <div className="mb-4 w-[400px] h-[600px] bg-white rounded-2xl shadow-2xl border border-gray-200 flex flex-col overflow-hidden transition-all duration-300 transform origin-bottom-right">
```
Sostituire con:
```tsx
                <div className="mb-4 w-[400px] h-[600px] bg-white dark:bg-gray-800 rounded-2xl shadow-2xl border border-gray-200 dark:border-gray-700 flex flex-col overflow-hidden transition-all duration-300 transform origin-bottom-right">
```

Trovare:
```tsx
                    <div className="flex-1 overflow-y-auto p-4 space-y-4 bg-gray-50/50">
```
Sostituire con:
```tsx
                    <div className="flex-1 overflow-y-auto p-4 space-y-4 bg-gray-50/50 dark:bg-gray-900/50">
```

Trovare (bolla assistente):
```tsx
                                        : 'bg-white text-gray-800 border border-gray-100 rounded-tl-none'
```
Sostituire con:
```tsx
                                        : 'bg-white dark:bg-gray-700 text-gray-800 dark:text-gray-200 border border-gray-100 dark:border-gray-600 rounded-tl-none'
```

Trovare (label "Assistente" e "Tu"):
```tsx
                                                <span className="text-[10px] font-bold text-gray-400 uppercase tracking-tighter">Assistente</span>
```
Sostituire con:
```tsx
                                                <span className="text-[10px] font-bold text-gray-400 dark:text-gray-500 uppercase tracking-tighter">Assistente</span>
```

Trovare:
```tsx
                                                <span className="text-[10px] font-bold text-gray-400 uppercase tracking-tighter">Tu</span>
```
Sostituire con:
```tsx
                                                <span className="text-[10px] font-bold text-gray-400 dark:text-gray-500 uppercase tracking-tighter">Tu</span>
```

Trovare:
```tsx
                                    <span className="text-[9px] text-gray-400 mt-1 px-1">
```
Sostituire con:
```tsx
                                    <span className="text-[9px] text-gray-400 dark:text-gray-500 mt-1 px-1">
```

Trovare (bolla loading):
```tsx
                                <div className="bg-white border border-gray-100 rounded-2xl rounded-tl-none px-4 py-3 shadow-sm">
```
Sostituire con:
```tsx
                                <div className="bg-white dark:bg-gray-700 border border-gray-100 dark:border-gray-600 rounded-2xl rounded-tl-none px-4 py-3 shadow-sm">
```

Trovare (input area):
```tsx
                    <div className="p-4 bg-white border-t border-gray-100">
```
Sostituire con:
```tsx
                    <div className="p-4 bg-white dark:bg-gray-800 border-t border-gray-100 dark:border-gray-700">
```

Trovare (input testo):
```tsx
                                className="w-full pl-4 pr-12 py-3 bg-gray-50 border-none rounded-xl text-sm focus:ring-2 focus:ring-indigo-500/20 transition-all placeholder:text-gray-400"
```
Sostituire con:
```tsx
                                className="w-full pl-4 pr-12 py-3 bg-gray-50 dark:bg-gray-700 dark:text-gray-100 dark:placeholder:text-gray-400 border-none rounded-xl text-sm focus:ring-2 focus:ring-indigo-500/20 transition-all placeholder:text-gray-400"
```

Trovare:
```tsx
                        <p className="text-[9px] text-center text-gray-400 mt-3 uppercase tracking-widest font-medium">
```
Sostituire con:
```tsx
                        <p className="text-[9px] text-center text-gray-400 dark:text-gray-500 mt-3 uppercase tracking-widest font-medium">
```

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/GoogleCalendarBanner.tsx frontend/src/components/DocumentChatbot.tsx
git commit -m "feat: dark mode — GoogleCalendarBanner and DocumentChatbot"
```

---

## Task 10: Modali dark mode (ClienteFormModal, ContrattoFormModal, UploadDocumentoModal, ClassificazioneModal)

I 4 modali condividono lo stesso schema: overlay `bg-black bg-opacity-50` (ok), container `bg-white rounded-lg shadow-xl`, header con `border-b border-gray-200`, labels `text-gray-700`, input con `border-gray-300`, bottoni cancel/confirm.

**Files:**
- Modify: `frontend/src/components/ClienteFormModal.tsx`
- Modify: `frontend/src/components/ContrattoFormModal.tsx`
- Modify: `frontend/src/components/UploadDocumentoModal.tsx`
- Modify: `frontend/src/components/ClassificazioneModal.tsx`

- [ ] **Step 1: Dark mode ClienteFormModal**

Trovare:
```tsx
      <div className="relative bg-white rounded-lg shadow-xl max-w-md w-full max-h-[90vh] overflow-y-auto">
        <div className="flex items-center justify-between p-6 border-b border-gray-200">
          <h3 className="text-lg font-medium text-gray-900">
```
Sostituire con:
```tsx
      <div className="relative bg-white dark:bg-gray-800 rounded-lg shadow-xl max-w-md w-full max-h-[90vh] overflow-y-auto">
        <div className="flex items-center justify-between p-6 border-b border-gray-200 dark:border-gray-700">
          <h3 className="text-lg font-medium text-gray-900 dark:text-gray-100">
```

Trovare:
```tsx
            className="text-gray-400 hover:text-gray-600"
```
Sostituire con:
```tsx
            className="text-gray-400 dark:text-gray-500 hover:text-gray-600 dark:hover:text-gray-300"
```

Trovare:
```tsx
            <div className="p-3 text-sm text-red-600 bg-red-50 rounded-md">
```
Sostituire (replace_all: true — anche in altri modali):
```tsx
            <div className="p-3 text-sm text-red-600 dark:text-red-400 bg-red-50 dark:bg-red-900/20 rounded-md">
```

Trovare (replace_all: true — tutte le label in ClienteFormModal):
```tsx
              className="block text-sm font-medium text-gray-700 mb-1">
```
Sostituire con:
```tsx
              className="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">
```

Trovare (replace_all: true — tutti gli input/select):
```tsx
                className="block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm"
```
Sostituire con:
```tsx
                className="block w-full rounded-md border-gray-300 dark:border-gray-600 dark:bg-gray-700 dark:text-gray-100 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm"
```

Trovare:
```tsx
            <p className="text-xs text-gray-500 -mt-2">** Almeno uno dei due è obbligatorio</p>
```
Sostituire con:
```tsx
            <p className="text-xs text-gray-500 dark:text-gray-400 -mt-2">** Almeno uno dei due è obbligatorio</p>
```

Trovare (bottone annulla):
```tsx
              className="px-4 py-2 text-sm font-medium text-gray-700 bg-white border border-gray-300 rounded-md shadow-sm hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500"
```
Sostituire con:
```tsx
              className="px-4 py-2 text-sm font-medium text-gray-700 dark:text-gray-200 bg-white dark:bg-gray-700 border border-gray-300 dark:border-gray-600 rounded-md shadow-sm hover:bg-gray-50 dark:hover:bg-gray-600 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500"
```

- [ ] **Step 2: Dark mode ContrattoFormModal**

Stessa struttura di ClienteFormModal. Applicare:
- `relative bg-white rounded-lg shadow-xl` → aggiungere `dark:bg-gray-800`
- `border-b border-gray-200` → `dark:border-gray-700`
- `text-gray-900` (titolo) → `dark:text-gray-100`
- `text-gray-400 hover:text-gray-600` (chiudi) → dark variants
- `text-sm text-red-600 bg-red-50` (errore) → dark variants
- `text-sm font-medium text-gray-700 mb-1` (labels, replace_all) → `dark:text-gray-300`
- `border-gray-300` (inputs/selects/textarea, replace_all) → dark variants
- bottone annulla → dark variants

- [ ] **Step 3: Dark mode UploadDocumentoModal**

Stessa struttura. Applicare tutti gli stessi cambiamenti.

In più, il file input ha una classe speciale:
```tsx
              className="block w-full text-sm text-gray-500 file:mr-4 file:py-2 file:px-4 file:rounded-md file:border-0 file:text-sm file:font-medium file:bg-indigo-50 file:text-indigo-700 hover:file:bg-indigo-100"
```
Sostituire con:
```tsx
              className="block w-full text-sm text-gray-500 dark:text-gray-400 file:mr-4 file:py-2 file:px-4 file:rounded-md file:border-0 file:text-sm file:font-medium file:bg-indigo-50 dark:file:bg-indigo-900/30 file:text-indigo-700 dark:file:text-indigo-400 hover:file:bg-indigo-100 dark:hover:file:bg-indigo-900/50"
```

Il select disabilitato:
```tsx
              className="block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm disabled:bg-gray-100 disabled:cursor-not-allowed"
```
Sostituire con:
```tsx
              className="block w-full rounded-md border-gray-300 dark:border-gray-600 dark:bg-gray-700 dark:text-gray-100 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm disabled:bg-gray-100 dark:disabled:bg-gray-600 disabled:cursor-not-allowed"
```

- [ ] **Step 4: Dark mode ClassificazioneModal (leggere e adattare)**

ClassificazioneModal ha la stessa struttura. Applicare:
- Container modale: aggiungere `dark:bg-gray-800`
- Header border: `dark:border-gray-700`
- Titoli: `dark:text-gray-100`
- Labels: `dark:text-gray-300`
- Select/input: dark variants
- Error div: `dark:bg-red-900/20 dark:text-red-400`
- Footer border/buttons: dark variants

- [ ] **Step 5: Commit**

```bash
git add frontend/src/components/ClienteFormModal.tsx frontend/src/components/ContrattoFormModal.tsx frontend/src/components/UploadDocumentoModal.tsx frontend/src/components/ClassificazioneModal.tsx
git commit -m "feat: dark mode — modal components (cliente, contratto, upload, classificazione)"
```

---

## Task 11: Omnibox + PdfDrawer dark mode

**Files:**
- Modify: `frontend/src/components/Omnibox.tsx`
- Modify: `frontend/src/components/PdfDrawer.tsx`

- [ ] **Step 1: Dark mode Omnibox**

Trovare (input container):
```tsx
        flex items-center gap-2 px-3 py-2 rounded-lg border bg-white transition-all duration-150
        ${isOpen || isFocused
          ? 'border-slate-400 ring-2 ring-slate-800/8 shadow-sm'
          : 'border-gray-200 hover:border-gray-300'
        }
```
Sostituire con:
```tsx
        flex items-center gap-2 px-3 py-2 rounded-lg border bg-white dark:bg-gray-700 transition-all duration-150
        ${isOpen || isFocused
          ? 'border-slate-400 dark:border-gray-500 ring-2 ring-slate-800/8 shadow-sm'
          : 'border-gray-200 dark:border-gray-600 hover:border-gray-300 dark:hover:border-gray-500'
        }
```

Trovare:
```tsx
          className="flex-1 text-sm text-slate-700 placeholder-gray-400 bg-transparent outline-none min-w-0"
```
Sostituire con:
```tsx
          className="flex-1 text-sm text-slate-700 dark:text-gray-200 placeholder-gray-400 dark:placeholder-gray-500 bg-transparent outline-none min-w-0"
```

Trovare (dropdown):
```tsx
        <div className="absolute top-full left-0 right-0 mt-1.5 bg-white border border-gray-100 rounded-xl shadow-xl z-40 overflow-hidden">
```
Sostituire con:
```tsx
        <div className="absolute top-full left-0 right-0 mt-1.5 bg-white dark:bg-gray-800 border border-gray-100 dark:border-gray-700 rounded-xl shadow-xl z-40 overflow-hidden">
```

Trovare:
```tsx
            <div className="px-4 py-6 text-center text-sm text-gray-400">
```
Sostituire con:
```tsx
            <div className="px-4 py-6 text-center text-sm text-gray-400 dark:text-gray-500">
```

Trovare:
```tsx
            <ul className="divide-y divide-gray-50">
```
Sostituire con:
```tsx
            <ul className="divide-y divide-gray-50 dark:divide-gray-700">
```

Trovare (item active/hover):
```tsx
                        ${isActive ? 'bg-slate-50' : 'hover:bg-gray-50'}
```
Sostituire con:
```tsx
                        ${isActive ? 'bg-slate-50 dark:bg-gray-700' : 'hover:bg-gray-50 dark:hover:bg-gray-700/50'}
```

Trovare:
```tsx
                      <div className="flex-shrink-0 w-8 h-8 rounded-md bg-slate-100 flex items-center justify-center">
                        <svg className="w-4 h-4 text-slate-500"
```
Sostituire con:
```tsx
                      <div className="flex-shrink-0 w-8 h-8 rounded-md bg-slate-100 dark:bg-gray-600 flex items-center justify-center">
                        <svg className="w-4 h-4 text-slate-500 dark:text-gray-400"
```

Trovare:
```tsx
                          <span className="text-sm font-medium text-slate-800 truncate max-w-[180px]"
```
Sostituire con:
```tsx
                          <span className="text-sm font-medium text-slate-800 dark:text-gray-200 truncate max-w-[180px]"
```

Trovare:
```tsx
                        <div className="text-xs text-gray-400 mt-0.5 truncate">{clienteNome}</div>
```
Sostituire con:
```tsx
                        <div className="text-xs text-gray-400 dark:text-gray-500 mt-0.5 truncate">{clienteNome}</div>
```

- [ ] **Step 2: Dark mode PdfDrawer**

Trovare (drawer panel):
```tsx
                className={`fixed top-0 right-0 h-full z-[60] flex flex-col bg-white shadow-2xl border-l border-gray-200 transition-transform duration-300 ease-in-out w-full md:w-[45vw] ${isOpen ? 'translate-x-0' : 'translate-x-full'
```
Sostituire con:
```tsx
                className={`fixed top-0 right-0 h-full z-[60] flex flex-col bg-white dark:bg-gray-800 shadow-2xl border-l border-gray-200 dark:border-gray-700 transition-transform duration-300 ease-in-out w-full md:w-[45vw] ${isOpen ? 'translate-x-0' : 'translate-x-full'
```

Trovare (drawer header):
```tsx
                        <div className="flex items-center justify-between px-5 py-4 border-b border-gray-200 bg-gray-50 flex-shrink-0">
```
Sostituire con:
```tsx
                        <div className="flex items-center justify-between px-5 py-4 border-b border-gray-200 dark:border-gray-700 bg-gray-50 dark:bg-gray-900/30 flex-shrink-0">
```

Trovare:
```tsx
                                <h2
                                    className="text-sm font-semibold text-gray-900 truncate"
```
Sostituire con:
```tsx
                                <h2
                                    className="text-sm font-semibold text-gray-900 dark:text-gray-100 truncate"
```

Trovare:
```tsx
                                onClick={onClose}
                                className="flex-shrink-0 p-1.5 rounded-lg text-gray-400 hover:text-gray-600 hover:bg-gray-200 transition-colors"
```
Sostituire con:
```tsx
                                onClick={onClose}
                                className="flex-shrink-0 p-1.5 rounded-lg text-gray-400 dark:text-gray-500 hover:text-gray-600 dark:hover:text-gray-300 hover:bg-gray-200 dark:hover:bg-gray-700 transition-colors"
```

Trovare (viewer PDF area):
```tsx
                        <div className="flex-shrink-0 bg-gray-100 border-b border-gray-200" style={{ height: '58%' }}>
```
Sostituire con:
```tsx
                        <div className="flex-shrink-0 bg-gray-100 dark:bg-gray-900 border-b border-gray-200 dark:border-gray-700" style={{ height: '58%' }}>
```

Trovare (loading text):
```tsx
                                    <div className="flex flex-col items-center justify-center h-full gap-3 text-gray-500">
```
Sostituire con:
```tsx
                                    <div className="flex flex-col items-center justify-center h-full gap-3 text-gray-500 dark:text-gray-400">
```

Trovare (metadata panel — la sezione sotto il viewer):
Il PdfDrawer ha una sezione info documento in basso. Aggiungere dark classes alle label/valori che usano `text-gray-500`, `text-gray-900`, `border-gray-200`, `bg-gray-50`.

Trovare nel drawer il metadata section:
```tsx
                        <div className="flex-1 overflow-y-auto p-5 space-y-4">
```
(questa sezione non ha classi colore proprie — le classi specifiche sono sugli elementi figli)

Per i testi nel drawer:
Trovare (replace_all: true — label campo):
```tsx
                            <span className="text-xs font-semibold text-gray-400 uppercase tracking-wide">
```
Sostituire con:
```tsx
                            <span className="text-xs font-semibold text-gray-400 dark:text-gray-500 uppercase tracking-wide">
```

Trovare (replace_all: true — valore campo):
```tsx
                            <span className="text-sm text-gray-700">
```
Sostituire con:
```tsx
                            <span className="text-sm text-gray-700 dark:text-gray-300">
```

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/Omnibox.tsx frontend/src/components/PdfDrawer.tsx
git commit -m "feat: dark mode — Omnibox and PdfDrawer"
```

---

## Task 12: Verifica finale

- [ ] **Step 1: Eseguire i test backend (nessun cambio backend)**

```bash
cd backend && python3 -m pytest tests/ -x -q
```
Expected: tutti i test passano (69+).

- [ ] **Step 2: Verificare il build frontend (nessun errore TypeScript)**

```bash
cd frontend && npm run build 2>&1 | tail -20
```
Expected: `built in Xs` senza errori TypeScript.

- [ ] **Step 3: Push**

```bash
git push origin main
```

---

## Note di contesto

- Il `h-full` su `html` e `body` in `index.css` è già presente — ok con `color-scheme: dark`
- La sidebar (`bg-slate-800`) è già scura di default — non richiede varianti dark
- I badge colorati (`bg-red-100 text-red-700`, `bg-green-100 text-green-800`, etc.) non vengono modificati per ora: rimangono colorati in entrambi i temi, il contrasto è sufficiente
- `DocumentiPage` ha più occorrenze di classi: usare `replace_all: true` dove possibile
- `ClassificazioneModal.tsx` va letto completamente prima di applicare le modifiche (non inclusa la JSX in questo piano)
- `PdfDrawer.tsx` ha elementi figli con varianti sconosciute — durante l'esecuzione, leggere il file completo e applicare dark classes a tutti i testi `text-gray-XXX` e sfondi `bg-grayXX`
